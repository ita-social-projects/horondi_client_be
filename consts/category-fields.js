const PRODUCTS = 'products';
const _ID = '_id';
const CATEGORY = 'category';
const FROM_PRODUCTS = 'fromProducts';
const PURCHASED_COUNT = '$fromProducts.purchasedCount';

module.exports = {
  PRODUCTS,
  _ID,
  CATEGORY,
  FROM_PRODUCTS,
  PURCHASED_COUNT,
};
