extends template-en
block styles 
    link(rel="stylesheet", href="css/confirm-email.css")
block subheaderText
    p.registrationText You were successfully signed up
block content
    div.contentRegistrationText
        p.congrats Congratulations! #[br] You were successfully signed up on HORONDI, the only thing left is to confirm your e-mail:
        button.confirm
            a(href=frontendUrl + `/confirmation/` + token) Confirm e-mail
        p.link or follow the link:
        a.alink(href=frontendUrl + `/confirmation/` + token) #{frontendUrl + `/confirmation/` + token}