extends template
block subheaderText
    p.registrationText Реєстрація Адміністратора
block content
    div.contentRegistrationText
        p.congrats Вітаємо! #[br] Щоб зареєструватись як адміністратор, підтвердіть пошту:
        button.confirm
            a(href=adminUrl + `/confirmation/` + token) Підтвердити e-mail
        p.link або перейдіть за посиланням нижче:
        a.alink(href=adminUrl + `/confirmation/` + token) #{adminUrl + `/confirmation/` + token}
