const Restriction = require('./restriction.model');

const {
  restrictionMessages: { NOT_FOUND, ALREADY_EXISTS },
} = require('../../consts/restriction.messages');

// class RestrictionService = {

// }

// module.exports = new RestrictionService();
